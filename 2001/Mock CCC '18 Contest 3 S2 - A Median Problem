from statistics import median

loop = int(input(''))
medians = []

for i in range(loop):
    nums = input('').split(' ')
    nums = [int(num) for num in nums]
    median_value = median(nums)
    medians.append(int(median_value))

sorted(medians)
median_value = median(medians)
print(median_value)
